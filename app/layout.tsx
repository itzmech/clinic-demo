import type { Metadata } from 'next'
import { Playfair_Display, Inter } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const playfair = Playfair_Display({ 
  subsets: ["latin"],
  variable: '--font-serif',
  display: 'swap',
});

const inter = Inter({ 
  subsets: ["latin"],
  variable: '--font-sans',
  display: 'swap',
});

export const metadata: Metadata = {
  title: 'Dr. Priya Sharma | Consultant Physician - Vijayawada',
  description: 'Dr. Priya Sharma is a trusted Consultant Physician in Vijayawada with 15+ years of experience. Specializing in General Medicine, Diabetes Management, Hypertension, and Preventive Healthcare.',
  keywords: ['doctor', 'physician', 'Vijayawada', 'diabetes', 'hypertension', 'general medicine'],
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className={`${playfair.variable} ${inter.variable} scroll-smooth bg-background`}>
      <body className="font-sans antialiased">
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
